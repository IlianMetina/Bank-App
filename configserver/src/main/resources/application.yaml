server:
  port: 8888

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IlianMetina/BankApp-Config.git
          clone-on-start: true
          default-label: master
          username: ${GIT_USERNAME}
          password: ${GIT_TOKEN}